alter session set current_schema=bank;


SELECT TRANS_DT,
SUBSTR(TRANS_DESC,1,20) AS TRANS_DESC,
TRANS_AMT
FROM BANK_TRANSACTION
WHERE UPPER(TRANS_TYPE) LIKE '%CR%' AND 
TO_CHAR(TRANS_DT,'MM-YY') = '06-10';
