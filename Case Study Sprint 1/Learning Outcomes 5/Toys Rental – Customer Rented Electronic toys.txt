alter session set current_schema = mt_toy;
SET heading OFF

SELECT CUSTOMER_ID, CUSTOMER_NAME, CITY
FROM CUSTOMER
WHERE CUSTOMER_ID IN (SELECT CUSTOMER_ID FROM
TOY_RENTAL
WHERE TOY_ID IN (
SELECT TOY_ID 
FROM TOY
WHERE UPPER(TOY_TYPE) = 'ELECTRONICS'
))
ORDER BY CUSTOMER_ID;
