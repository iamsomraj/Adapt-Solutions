alter session set current_schema = mt_toy;
SET heading OFF
SELECT CUSTOMER_ID
FROM (
SELECT CUSTOMER_ID, COUNT(TOY_ID)
FROM TOY_RENTAL TOY
GROUP BY CUSTOMER_ID
ORDER BY COUNT(TOY_ID) DESC )
WHERE ROWNUM = 1;