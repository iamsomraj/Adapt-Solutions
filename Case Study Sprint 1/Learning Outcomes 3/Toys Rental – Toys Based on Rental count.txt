alter session set current_schema = mt_toy;
SET heading OFF
SELECT CUSTOMER.CUSTOMER_NAME FROM 
( SELECT DISTINCT CUSTOMER_ID, STATUS, COUNT(CUSTOMER_ID)
FROM TOY_RENTAL
GROUP BY CUSTOMER_ID, STATUS
HAVING COUNT(CUSTOMER_ID) > 2 ) CUST, CUSTOMER
WHERE CUST.CUSTOMER_ID = CUSTOMER.CUSTOMER_ID;