alter session set current_schema = mt_toy;
SET heading OFF
SELECT TOY.*
FROM TOY
WHERE TOY_ID IN (
SELECT TOY_ID
FROM TOY_RENTAL
GROUP BY TOY_ID
HAVING COUNT(TOY_ID) = (
SELECT MAX(CONT) 
FROM (
SELECT TOY_ID, COUNT(TOY_ID) AS CONT
FROM TOY_RENTAL
GROUP BY TOY_ID
)));