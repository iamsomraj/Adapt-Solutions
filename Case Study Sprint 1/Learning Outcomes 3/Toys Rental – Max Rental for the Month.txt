alter session set current_schema = mt_toy;
SET heading OFF

SELECT * FROM ( 
SELECT CUSTOMER_ID, SUM(TOTAL_AMOUNT)
FROM TOY_RENTAL TOY 
WHERE TO_CHAR(RENTAL_START_DATE,'MM-YY') = '06-20' OR
TO_CHAR(RENTAL_END_DATE,'MM-YY') = '06-20'
GROUP BY CUSTOMER_ID
ORDER BY 2 DESC )
WHERE ROWNUM = 1;